# Jetson YOLO 排错手册

按报错关键字跳，不要从头读。

## 1. `Illegal instruction` / 一 import torch 就挂

板子架构和 wheel 不匹配。Jetson 不要用 x86 的 `pip install torch`。

- JetPack 5：用 NVIDIA 提供的 PyTorch wheel（jp5）
- JetPack 6：用 jp6 对应 wheel
- 不要从 Windows 拷 `.venv` 过来

## 2. `engine` 加载失败 / `deserialize` error

Engine 和当前 TensorRT **大版本必须一致**，而且要在**同一块 Jetson**上导出。

- Xavier 上导的 engine，Orin 上不能用
- JetPack 5 导的，升到 JetPack 6 后要重导
- 重新跑 `02_export_tensorrt.py`

## 3. FPS 只有 2～5，热得发烫

```bash
sudo nvpmodel -m 0          # Orin Nano 常见最大性能档，以你板子文档为准
sudo jetson_clocks
sudo tegrastats             # 看是否一直 99% GPU、温度 >80°C
```

再检查：

- 是不是还在用 `.pt` 而不是 `.engine`
- `imgsz` 是不是 1280（先改 640）
- 有没有接显示器拖垮 GPU

## 4. `cv2.imshow` 报错 / 没有界面

SSH 进去默认没有 DISPLAY。用：

```bash
python3 scripts/04_usb_camera_detect.py --weights yolov8n.engine --no-show --save out.mp4
```

或插上显示器再跑。

## 5. 摄像头打不开 `CAMERA_ERROR` / `/dev/video0` 不存在

```bash
ls /dev/video*
v4l2-ctl --list-devices
```

USB 相机试试 `--source 1`。CSI 相机不要用 OpenCV `VideoCapture(0)` 硬开，走 `nvarguscamerasrc` 或厂商 ROS 驱动。

## 6. `ModuleNotFoundError: cv_bridge`

```bash
sudo apt install ros-humble-cv-bridge ros-humble-vision-opencv
```

发行版换成你自己的（noetic / iron / jazzy）。

## 7. ROS 能启动但画面是空的 / 延迟爆炸

- 话题名不对：`ros2 topic list` + `ros2 topic hz /your/image`
- 图像是 `compressed`：节点默认订 raw。先 `image_transport` 解压，或改 launch 里的话题
- 队列堆满：降低发布频率，或像本包节点这样「只处理最新帧」

## 8. INT8 精度崩了，类别全乱

INT8 需要校准数据。没有代表数据集就用 FP16：

```bash
python3 scripts/02_export_tensorrt.py --weights your.pt --half
```

真要 INT8，加 `--int8 --data coco128.yaml`（或你自己的 data yaml）。

## 9. `error: failed to load nv_tegra_release`

你可能在 x86 笔记本上跑了检查脚本。部署脚本以 Jetson 为准；笔记本只能训练和导出 ONNX，不要在笔记本上导 TensorRT engine 再拷到板子。

## 10. pip 把系统包搅烂了

Jetson 上优先：

```bash
python3 -m pip install --upgrade pip
python3 -m pip install ultralytics
```

不要乱卸 `numpy`。若 apt 的 OpenCV 和 pip 的 OpenCV 冲突，推理脚本只用 `opencv-python-headless`。

## 11. 显存不够 / `out of memory`

换 n 模型、缩小 `imgsz`、关掉其他 Chrome / 训练进程。Orin Nano 8GB 上不要同时开 3 个 1280 的 engine。

## 12. 时区 / 权限导致脚本不能执行

```bash
chmod +x scripts/*.sh
```

需要 `tegrastats`、`nvpmodel` 时用有 sudo 的账号。
