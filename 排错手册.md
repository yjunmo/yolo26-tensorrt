# 排错

## `deserialize` / engine 加载失败

engine 必须在**同一块板、同一套 JetPack**上导出。Xavier 的 engine 不能在 Orin 上用；JetPack 5 的不能直接给 JetPack 6 用。

## 输出维度报错（`want [1, max_det, 6]`）

只接受 YOLO26 端到端导出。用 `yolo export ... format=onnx` 再 `trtexec --fp16`。旧版 Ultralytics `format=engine` 图结构不同。

## Illegal instruction / import torch 崩溃

Jetson 上装了 x86 的 PyTorch。ONNX 可以在 PC 上导，**TensorRT engine 仍要在板子上编**。

## FPS 很低 / 过热降频

```bash
sudo nvpmodel -m 0    # 以你模块的最大性能档为准
sudo jetson_clocks
sudo tegrastats
```

确认没有还在用 Python 跑 `.pt`。

## SSH 下 `imshow` 失败

看 ROS 话题 `/yolo26/image`，或接显示器。

## 没有 `/dev/video*`

换摄像头编号，CSI 请走厂商 ROS 驱动，不要硬开 `VideoCapture(0)`。

## 缺少 `cv_bridge`

```bash
sudo apt install ros-humble-cv-bridge ros-humble-vision-opencv
```

## INT8 类别全乱

本仓库导出脚本走 FP16。INT8 需要校准集，没有就不要开。

## CMake CUDA 架构

Orin 默认 `87`。其他 GPU：`-DCMAKE_CUDA_ARCHITECTURES=86`。x86 TensorRT：`-DTENSORRT_ROOT=/path/to/TensorRT`。
